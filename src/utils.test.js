import { hit, isSunk } from "./utils.js"

//hit tests
test('returns an input', () => {
    expect(hit(1)).toEqual(1);
});

//isSunk tests
test('returns an input', () => {
    expect(hit(1)).toEqual(1);
});

